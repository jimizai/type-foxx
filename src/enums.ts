export enum RequestMethod {
	GET = 0,
	POST,
	PUT,
	DELETE,
	PATCH,
	OPTIONS,
	HEAD,
	ALL
}

export enum RouteParamtypes {
	REQUEST,
	RESPONSE,
	NEXT,
	BODY,
	QUERY,
	PARAM,
	HEADERS,
	SESSION,
	FILE,
	FILES,
	HOST,
	IP
}
